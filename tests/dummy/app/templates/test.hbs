{{test-component isShown=isShown
                 isLarge=isLarge
                 margin=margin
                 marginTop=marginTop
                 width=width
                 height=height}}

{{outlet}}
